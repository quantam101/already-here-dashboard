# CI requirements — flexible constraints, no heavy ML/GPU packages.
# The full frozen lockfile (requirements.txt) is for production Docker builds only.
# These packages are sufficient for all unit/integration/regression tests.

# --- Web framework ---
fastapi>=0.110.0
uvicorn>=0.25.0
starlette>=0.37.0
python-multipart>=0.0.9

# --- Validation ---
pydantic>=2.5.0
email-validator>=2.0.0

# --- Database ---
motor>=3.3.0
pymongo>=4.5.0
aiosqlite>=0.19.0

# --- Auth ---
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
PyJWT>=2.6.0

# --- HTTP ---
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.9.0

# --- Config ---
python-dotenv>=1.0.0
PyYAML>=6.0

# --- LLM clients ---
openai>=1.0.0
huggingface_hub>=0.20.0
google-generativeai>=0.5.0
litellm>=1.80.0

# --- Payments / AWS ---
stripe>=7.0.0
boto3>=1.28.0

# --- Scheduling ---
tenacity>=8.2.0
psutil>=5.9.0

# --- Data ---
pandas>=2.0.0
numpy>=1.24.0

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.21.0

# --- Misc ---
Jinja2>=3.1.0
python-dateutil>=2.8.0
tiktoken>=0.5.0
rich>=13.0.0
tqdm>=4.65.0
packaging>=23.0
click>=8.0.0
filelock>=3.12.0
anyio>=3.7.0
sniffio>=1.3.0
certifi>=2023.0.0
charset-normalizer>=3.0.0
idna>=3.4
urllib3>=2.0.0
cryptography>=41.0.0
PyYAML>=6.0
regex>=2023.0.0
